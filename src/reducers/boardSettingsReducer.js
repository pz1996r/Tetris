const initialState = {
    rows: 18,
    columns: 26
}
export const boardSettingsReducer = (state = { ...initialState }, action) => {
    switch (action.type) {
        default: return state;
    }
}

